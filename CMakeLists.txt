message("CMAKE BUILD SYSTEM FOR CSOUND-EXTENDED")
message("Execute \"cd dependencies;./update-dependencies.sh;cd..\" before building!")
message("This does not need to be repeated for each build, only when dependencies change.")
message("There are NO configuration options in this build. It should just build everything.")
project(csound-extended)
