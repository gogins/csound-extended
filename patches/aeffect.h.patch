--- dependencies/VST_SDK/VST2_SDK/pluginterfaces/vst2.x/aeffect.h	2017-11-08 07:49:41.000000000 -0500
+++ patches/aeffect.h	2018-01-23 17:27:06.828298242 -0500
@@ -59,21 +59,25 @@
 		#pragma options align=power
 	#else
 		#pragma options align=mac68k
 	#endif
 	#define VSTCALLBACK
 #elif defined __BORLANDC__
 	#pragma -a8
 	#pragma options push -a8
 #elif defined(__GNUC__)
     #pragma pack(push,8)
+#if defined(WIN32)
     #define VSTCALLBACK __cdecl
+#elif
+    #define VSTCALLBACK
+#endif
 #elif defined(WIN32) || defined(__FLAT__) || defined CBUILDER
 	#pragma pack(push)
 	#pragma pack(8)
 	#define VSTCALLBACK __cdecl
 #else
 	#define VSTCALLBACK
 #endif
 //-------------------------------------------------------------------------------------------------------
 
 #include <string.h>	// for strncpy
